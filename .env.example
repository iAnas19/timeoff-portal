# Server-only — required for API routes / SSR (not exposed to browser)
HCM_API_URL=http://localhost:3000/api/hcm
HCM_API_TIMEOUT_MS=8000

# Client-safe (inlined in browser bundle)
NEXT_PUBLIC_APP_ENV=development
# Optional client timeout override (defaults to 8000 if unset)
# NEXT_PUBLIC_HCM_API_TIMEOUT_MS=8000

# Mock HCM auth header (Phase 3+): x-mock-auth: demo